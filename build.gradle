allprojects {
    buildscript {
        repositories {
            jcenter()
            mavenCentral()
        }
    }
    repositories {
        mavenCentral()
    }
}

ext {
    androidPlugin = 'com.android.tools.build:gradle:2.0.0-alpha1'

    minSdkVersion = 16
    compileSdkVersion = 23
    targetSdkVersion = 23
    buildToolsVersion = '23.0.2'
}
